'use strict';

NguberMember.factory('geoCode', [
  '$http',
  function ($http) {
    var model = {      
    };
    
//    model.ToGeoCode = function (address) {
//      return $http.get(''
//    }
  }
]);